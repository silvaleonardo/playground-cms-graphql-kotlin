spring:
  graphql:
    graphiql:
      enabled: true
  datasource:
    url: jdbc:mysql://localhost:3304/cms?useTimezone=true&serverTimezone=UTC
    username: cms
    password: cms
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format-sql: true